void main(){

  //dynamic keyword is used to declare a variable whose type can be changed
  dynamic variable = 100;
  print(variable);

  variable = true;
  print(variable);

  variable = 'Eko';
  print(variable);

}